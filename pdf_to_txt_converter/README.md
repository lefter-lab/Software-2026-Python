Конвертира PDF файл в TXT (UTF-8), запазва кирилица.

Функция: `pdf_to_txt(pdf_path, txt_path)` — извлича текст чрез `pdfminer` и записва в UTF-8 текстов файл.

Пример: `python pdf_to_txt_converter.py` (или извикване на `pdf_to_txt`).